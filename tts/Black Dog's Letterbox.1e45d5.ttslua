#include etiquette

wait_id = 0

function onLoad(save_state)
    startSorter()
end

function startSorter()
    wait_id = Wait.time(function() sortMail("Brown") end, sortTimer, -1)
end

function onDestroy()
    Wait.stop(wait_id)
end
