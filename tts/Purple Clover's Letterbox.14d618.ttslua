#include etiquette

wait_id = 0

function onLoad(save_state)
    wait_id = Wait.time(function() startSorter() end, 5 * startOffset)
end

function startSorter()
    wait_id = Wait.time(function() sortMail("Purple") end, sortTimer, -1)
end

function onDestroy()
    Wait.stop(wait_id)
end
