#include etiquette

wait_id = 0

function onLoad(save_state)
    wait_id = Wait.time(function() startSorter() end, 4 * startOffset)
end

function startSorter()
    wait_id = Wait.time(function() sortMail("Pink") end, sortTimer, -1)
end

function onDestroy()
    Wait.stop(wait_id)
end
